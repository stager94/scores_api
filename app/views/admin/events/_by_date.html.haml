= form_tag sync_by_date_admin_events_path, class: "filter_form" do |f|
  .filter_form_field
    = label :data, "Date", class: "label"
    = text_field_tag :data, nil, placeholder: "Date"
  .buttons
    = submit_tag "Synchronize"
